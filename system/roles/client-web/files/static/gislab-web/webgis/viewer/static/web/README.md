This directory contains only HTML templates

You should *NOT* modify `styles/` and `js/` files by hand, but you should go to 
`../../webgis` directory, fix the style and javascripts there and use `gulp` to
deploy here.


