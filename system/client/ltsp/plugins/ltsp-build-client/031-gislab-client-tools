# Install GIS.lab client tools

case "$MODE" in
    finalization)
	mkdir -p $ROOT/usr/local/gislab
	cp /vagrant/system/functions.sh $ROOT/usr/local/gislab/

	cp -a /vagrant/system/client/bin/* $ROOT/usr/local/bin/
    ;;
esac
