---

- hosts: all
  sudo: yes
  roles:
    - basic-server-configuration
    - user-accounts
    - service-syslog
    - service-logcheck
    - service-time
    - service-dns
    - service-ldap
    - service-mail
    - service-dhcp
    - service-default-web
    - service-files
    - service-chat
    - service-database
    - service-mapserver
    - service-load-balancer
    - service-statistics
    - user-data
    - service-worker
    - { role: service-client, when: "'{{ GISLAB_SUITE }}' == 'lab'" }
    - connection-forwarding
    - installation-done
