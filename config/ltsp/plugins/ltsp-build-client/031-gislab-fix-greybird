# Fix to Xubuntu Greybird theme which provides very thin window borders which are very
# difficult to grab and resize (taken from OSGeo Live CD). 

case "$MODE" in
    finalization)
	cp /vagrant/config/xfce4/greybird/*.* $ROOT/usr/share/themes/Greybird/xfwm4/
    ;;
esac
