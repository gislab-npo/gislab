# Run custom install script

case "$MODE" in
    finalization)
	cp /vagrant/$GISLAB_CLIENT_USER_INSTALL_SCRIPT $ROOT/tmp/

	chroot $ROOT /tmp/$(basename $GISLAB_CLIENT_USER_INSTALL_SCRIPT)

	rm $ROOT/tmp/$(basename $GISLAB_CLIENT_USER_INSTALL_SCRIPT)
    ;;
esac
