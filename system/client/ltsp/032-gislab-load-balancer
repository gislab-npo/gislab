case "$MODE" in
	finalization)
		if [ "$GISLAB_CLIENT_LOAD_BALANCER_ENABLED" == "yes" ]; then
			chroot $ROOT apt-get --assume-yes --force-yes install apache2 apache2-mpm-worker libapache2-mod-fastcgi qgis-mapserver unzip

			# apache
			mkdir -p $ROOT/var/www/default
			cp $GISLAB_ROOT/system/server/047-service-default-web/conf/index.html $ROOT/var/www/default/index.html

			cp $GISLAB_ROOT/system/server/047-service-default-web/conf/apache/ports.conf $ROOT/etc/apache2/ports.conf
			cp $GISLAB_ROOT/system/server/047-service-default-web/conf/apache/site-default $ROOT/etc/apache2/sites-available/default
			cp $GISLAB_ROOT/system/server/090-service-mapserver/conf/apache/site-mapserver $ROOT/etc/apache2/sites-available/mapserver

			# log error to syslog
			sed -i 's/^ErrorLog .*/ErrorLog "|\/usr\/bin\/logger -t apache -p daemon.warn"/' $ROOT/etc/apache2/apache2.conf

			chroot $ROOT a2enmod rewrite
			chroot $ROOT a2enmod expires
			chroot $ROOT a2ensite default
			chroot $ROOT a2ensite mapserver

			# serf
			chroot $ROOT /bin/bash -c "source /usr/local/gislab/functions.sh; gislab_serf_install"
			cp $GISLAB_ROOT/system/server/107-service-worker/bin/serf-member-*.sh $ROOT/usr/local/bin/
			chmod +x $ROOT/usr/local/bin/serf-member-*.sh

			cp $GISLAB_ROOT/system/server/107-service-worker/conf/serf/serf.conf $ROOT/etc/init/serf.conf
			cat << EOF > $ROOT/etc/init/serf-join.conf
description "Join a GIS.lab network"

start on runlevel [2345]
stop on runlevel [!2345]

task
respawn

script
sleep 5
exec /usr/local/bin/serf join $GISLAB_SERVER_IP
end script
EOF
		fi
	;;
esac
